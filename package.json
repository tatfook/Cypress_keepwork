{
  "name": "cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:run": "cypress run",
    "delete:reports": "rm cypress/results/* || true",    
    "report": "yarn run cypress run --browser chrome --record false --reporter mocha-multi-reporters --reporter-options configFile=reporter-config.json"
  },
  "author": "Duke & DuTing",
  "license": "ISC",
  "dependencies": {
    "randomstring": "^1.1.5",
    "cypress": "^3.1.5"
  },
  "devDependencies": {
    "mocha": "^5.2.0",
    "mocha-junit-reporter": "^1.21.0",
    "mocha-multi-reporters": "^1.1.7"
  }
}
